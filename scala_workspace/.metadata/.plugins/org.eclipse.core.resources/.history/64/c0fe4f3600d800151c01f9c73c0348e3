package week_two

object tail_sum {

    def sum(f: Int => Int, a: Int, b: Int): Int = {     //> Welcome to the Scala worksheet
    
    def loop(a: Int, acc: Int): Int = {
      if (a>b) acc
      else loop(a+1, f(a)+sum(b))
    }
    loop(1, 0)
  }
}