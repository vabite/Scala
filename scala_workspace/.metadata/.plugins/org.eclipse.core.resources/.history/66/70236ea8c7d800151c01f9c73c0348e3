package week_one

object Main extends App {
    def pascal(c: Int, r: Int): Int= if(c==0 || c==r) 1 else pascal(c-1, r)+pascal(c, r-1)
  	println("Pascal of col "+c+" and row "+r+" is "+pascal(1 , 3))        
}