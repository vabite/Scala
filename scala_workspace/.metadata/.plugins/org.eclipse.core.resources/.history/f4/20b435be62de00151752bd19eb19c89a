
object prove {
	def checkCol(col: Int, queens: List[Int]): Boolean={
		val deltaRows=queens.length
		if((queens contains col) || (col==queens.head-queens.length)) false
		else true
		}                                 //> checkCol: (col: Int, queens: List[Int])Boolean
}