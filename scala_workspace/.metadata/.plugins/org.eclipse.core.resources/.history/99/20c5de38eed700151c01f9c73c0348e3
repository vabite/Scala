package week_one

object tail_factorial {

	def factorialIter(){
	  def factorial(rest: Int, cum: Int): Int={
	  	if(rest==0) cum
	  	else factorial(rest-1, rest*cum)
	  	}                                              //> res0: Int = 1
	  }
  }